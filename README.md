# 🎮 База Даних для Ігор

База даних для зберігання інформації про ігри, героїв, їхні навички та предмети.

## ✨ Особливості

- ✅ Проста та зручна SQLite база даних
- ✅ Повний CRUD для ігор, героїв, навичок та предметів
- ✅ Пошук та статистика
- ✅ Експорт/імпорт даних у JSON
- ✅ Інтерактивний консольний інтерфейс
- ✅ Повні тести та приклади використання
- ✅ Не потребує встановлення додаткових пакетів

## 📋 Структура бази даних

### Таблиці:

1. **games** - інформація про ігри
   - id, name, description, release_date, genre

2. **heroes** - герої в іграх
   - id, game_id, name, hero_game_id, image, role, description

3. **hero_skills** - навички героїв
   - id, hero_id, skill_name, skill_description, cooldown, mana_cost, damage, skill_type

4. **items** - предмети в іграх
   - id, game_id, name, item_game_id, description, image, item_type, cost, stats

## 🚀 Встановлення

Не потрібно встановлювати додаткові пакети - використовується вбудований SQLite.

```bash
# Python 3.6+ вже має SQLite
python3 --version
```

## 💻 Використання

### Базовий приклад:

```python
from database import GameDatabase

# Створення та підключення
db = GameDatabase("my_games.db")
db.connect()
db.create_tables()

# Додавання гри
game_id = db.add_game(
    name="Dota 2",
    description="MOBA game",
    genre="MOBA"
)

# Додавання героя
hero_id = db.add_hero(
    game_id=game_id,
    name="Invoker",
    role="Intelligence",
    description="Маг з багатьма заклинаннями"
)

# Додавання навички
db.add_hero_skill(
    hero_id=hero_id,
    skill_name="Cold Snap",
    damage=50.0,
    mana_cost=100,
    cooldown=17.0
)

# Додавання предмета
db.add_item(
    game_id=game_id,
    name="Black King Bar",
    cost=4050,
    stats={"strength": 10, "damage": 24}
)

# Отримання даних
heroes = db.get_heroes_by_game(game_id, include_skills=True)
items = db.get_items_by_game(game_id)
stats = db.get_game_stats(game_id)

db.disconnect()
```

## 🧪 Тестування

### 1. Автоматичні тести

Запустіть повний набір тестів:

```bash
python3 test_database.py
```

Цей скрипт:
- ✅ Створить тестову базу даних `test_games.db`
- ✅ Додасть приклади ігор (Dota 2, League of Legends)
- ✅ Додасть героїв з навичками
- ✅ Додасть предмети з характеристиками
- ✅ Виведе всю інформацію
- ✅ Покаже статистику
- ✅ Протестує пошук

### 2. Приклад використання

Запустіть простий приклад:

```bash
python3 example_usage.py
```

Створить базу даних `my_games.db` з прикладом гри Overwatch.

### 3. Інтерактивний режим

Запустіть інтерактивний консольний інтерфейс:

```bash
python3 interactive.py
```

Меню інтерфейсу:
- 📖 Переглянути всі ігри
- ➕ Додати нову гру
- 👥 Переглянути героїв гри
- 🦸 Додати героя
- ⚔️ Додати навичку герою
- 🎒 Переглянути предмети гри
- 📦 Додати предмет
- 🔍 Пошук предметів
- 📊 Статистика гри

### 4. Експорт/Імпорт даних

```bash
python3 export_import.py
```

Можливості:
- Експортувати одну гру у JSON
- Експортувати всі ігри у JSON
- Імпортувати гру з JSON файлу

## 📖 Приклади

Запустіть приклад використання:

```bash
python3 example_usage.py
```

## 🔍 API методи

### Ігри:
- `add_game(name, description, release_date, genre)` - додати гру
- `get_game(game_id)` - отримати гру за ID
- `get_game_by_name(name)` - отримати гру за назвою
- `get_all_games()` - отримати всі ігри

### Герої:
- `add_hero(game_id, name, ...)` - додати героя
- `get_hero(hero_id, include_skills)` - отримати героя
- `get_heroes_by_game(game_id, include_skills)` - отримати героїв гри

### Навички:
- `add_hero_skill(hero_id, skill_name, ...)` - додати навичку

### Предмети:
- `add_item(game_id, name, ...)` - додати предмет
- `get_item(item_id)` - отримати предмет
- `get_items_by_game(game_id)` - отримати предмети гри
- `search_items(game_id, search_term)` - пошук предметів

### Статистика:
- `get_game_stats(game_id)` - статистика по грі

## 🗄️ Перегляд бази даних

Можете використати:
- **DB Browser for SQLite** - https://sqlitebrowser.org/
- **DBeaver** - https://dbeaver.io/
- Або команду: `sqlite3 games.db`

## 📝 Приклади запитів SQL

```sql
-- Всі герої з їхніми навичками
SELECT h.name as hero, s.skill_name, s.damage 
FROM heroes h 
LEFT JOIN hero_skills s ON h.id = s.hero_id;

-- Топ-5 найдорожчих предметів
SELECT name, cost FROM items 
ORDER BY cost DESC LIMIT 5;

-- Кількість героїв по ролям
SELECT role, COUNT(*) as count 
FROM heroes 
GROUP BY role;
```

## 🎯 Можливості розширення

- Додати систему рейтингів для героїв
- Зберігати білди (комбінації предметів)
- Додати систему контрпіків між героями
- Зберігати історію змін балансу
- Додати зв'язки між предметами (крафт)

## 📄 Ліцензія

Вільне використання для навчальних цілей.
