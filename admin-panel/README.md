# 🎮 Game Database Admin Panel

React адмін-панель для управління базою даних ігор, героїв та предметів.

## 📋 Можливості

- ✅ **Ігри**: Перегляд, додавання, редагування та видалення ігор
- ✅ **Герої**: Управління героями з їх ролями, складністю та здібностями
- ✅ **Предмети**: Управління предметами з статистикою та властивостями
- ✅ **REST API**: Повна підтримка CRUD операцій (Create, Read, Update, Delete)

## 🚀 Швидкий старт

### 1. Запустити API сервер

У першому терміналі:

```bash
cd /Users/alex/My/game_database
python3 api_server.py
```

API буде доступне на http://localhost:8080

### 2. Запустити React додаток

У другому терміналі:

```bash
cd /Users/alex/My/game_database/admin-panel
npm install    # Тільки перший раз
npm start
```

Адмін-панель відкриється на http://localhost:3000

## 🛠️ Технології

- **Frontend**: React 18.2.0
- **HTTP Client**: Axios 1.6.0
- **Backend API**: Flask REST API на порту 8080
- **База даних**: SQLite 3

## 📁 Структура проекту

```
admin-panel/
├── public/
│   └── index.html           # HTML шаблон
├── src/
│   ├── components/          # React компоненти
│   │   ├── GameList.js      # Список ігор
│   │   ├── GameForm.js      # Форма додавання/редагування гри
│   │   ├── HeroList.js      # Список героїв
│   │   ├── HeroForm.js      # Форма героя зі здібностями
│   │   ├── ItemList.js      # Список предметів
│   │   └── ItemForm.js      # Форма предмета зі статистикою
│   ├── App.js               # Головний компонент
│   ├── App.css              # Стилі
│   ├── index.js             # Точка входу
│   └── index.css            # Глобальні стилі
├── package.json             # Залежності
└── README.md               # Ця інструкція
```

## 🔌 API Endpoints

### Ігри
- `GET /api/games` - Список всіх ігор
- `GET /api/games/:id` - Отримати гру за ID
- `POST /api/games` - Створити нову гру
- `PUT /api/games/:id` - Оновити гру
- `DELETE /api/games/:id` - Видалити гру

### Герої
- `GET /api/games/:id/heroes` - Список героїв гри
- `GET /api/heroes/:id` - Отримати героя за ID
- `POST /api/heroes` - Створити нового героя
- `PUT /api/heroes/:id` - Оновити героя
- `DELETE /api/heroes/:id` - Видалити героя

### Предмети
- `GET /api/games/:id/items` - Список предметів гри
- `GET /api/items/:id` - Отримати предмет за ID
- `POST /api/items` - Створити новий предмет
- `PUT /api/items/:id` - Оновити предмет
- `DELETE /api/items/:id` - Видалити предмет

## 💡 Як використовувати

### Додати гру

1. Перейти на вкладку "📖 Ігри"
2. Натиснути "+ Додати гру"
3. Заповнити форму: назва, жанр, опис
4. Натиснути "Зберегти"

### Додати героя

1. Обрати гру в селекторі вгорі
2. Перейти на вкладку "🦸 Герої"
3. Натиснути "+ Додати героя"
4. Заповнити інформацію про героя
5. (Опційно) Додати здібності з cooldown та mana cost
6. Натиснути "Зберегти"

### Додати предмет

1. Обрати гру в селекторі вгорі
2. Перейти на вкладку "⚔️ Предмети"
3. Натиснути "+ Додати предмет"
4. Заповнити інформацію про предмет
5. (Опційно) Додати статистику (напр. strength: +10)
6. Натиснути "Зберегти"

## 🐛 Усунення проблем

### API сервер не запускається

```bash
# Переконайтесь що порт 8080 вільний
lsof -i :8080

# Якщо порт зайнятий, вбийте процес
kill -9 <PID>
```

### React додаток не запускається

```bash
# Видаліть node_modules і переінсталюйте
rm -rf node_modules package-lock.json
npm install
npm start
```

### Помилка "Address already in use"

Порт 3000 зайнятий. Запустіть на іншому порту:

```bash
PORT=3001 npm start
```

## 📝 Примітки

- API проксується через package.json: `"proxy": "http://localhost:8080"`
- Всі зміни зберігаються в `test_games.db`
- При видаленні гри, автоматично видаляються всі герої та предмети (CASCADE)
- При видаленні героя, автоматично видаляються його здібності

## 🎯 Подальший розвиток

- [ ] Аутентифікація користувачів
- [ ] Пагінація для великих списків
- [ ] Фільтри та сортування
- [ ] Експорт/Імпорт даних
- [ ] Візуалізація статистики
- [ ] Валідація форм
- [ ] Toast повідомлення замість alert()
